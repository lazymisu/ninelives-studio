---
import { Image } from "astro:assets";
import { services } from "../data/services";
import nlsImage from "../assets/nls.png";
---

<section
    id="home"
    class="w-full flex flex-col gap-8 px-4 md:px-12 xl:px-24 py-8 md:py-12 xl:py-20 mt-24 bg-background"
>
    <h1 class="sr-only">
        Estudio de mezcla, mastering y postproducción de audio ubicado en Buenos
        Aires, Argentina. Especializado en rock, indie y alternativo - Nine
        Lives Studio
    </h1>
    <div class="w-full">
        <h2
            class="hero -translate-x-10 opacity-0 text-[12vw] xl:text-[12vw] font-extrabold leading-none tracking-tight uppercase text-left text-accent"
        >
            NINE LIVES STUDIO
        </h2>
    </div>
    <div
        class="home translate-y-10 opacity-0 flex-1 w-full grid grid-cols-1 xl:grid-cols-3 gap-8 items-end place-items-end"
    >
        <div class="flex flex-col gap-4 self-end w-full">
            <h3 class="font-bold text-base xl:text-lg mb-1 text-primary">
                Servicios
            </h3>
            <ol class="flex flex-col gap-2">
                {
                    services.map((u) => (
                        <li class="flex items-center gap-3 rounded-xl p-4 text-sm xl:text-base bg-fill">
                            <span class="text-2xl">{u.icon}</span>
                            <h4 class="text-primary">{u.title}</h4>
                        </li>
                    ))
                }
            </ol>
        </div>
        <div class="flex flex-col self-end w-full">
            <p class="text-base xl:text-lg mb-3 text-primary">
                En Nine Lives Studio llevamos el sonido a otro nivel. Mezclas con claridad, masters con carácter y diseño sonoro pensado al detalle. Ya sea música, cine o contenido digital, cada proyecto se trabaja con enfoque técnico y sensibilidad artística.
            </p>
            <a
                href="#about"
                class="underline font-medium text-sm whitespace-nowrap text-accent"
                >Sobre nosotros</a
            >
        </div>
        <div
            class="flex justify-center items-center h-48 md:h-64 xl:h-80 w-full"
        >
            <Image
                src={nlsImage}
                alt="Nine Lives Studio Logo"
                class="max-w-full max-h-full object-contain"
            />
        </div>
    </div>
</section>

<script>
    import("gsap").then(({ gsap }) => {
        gsap.to(".hero", {
            x: 0,
            opacity: 1,
            duration: 1,
            ease: "back.out",
        });

        gsap.to(".home", {
            y: 0,
            opacity: 1,
            duration: 1,
            ease: "back.out",
        });
    });
</script>
